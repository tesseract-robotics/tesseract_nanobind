repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.10
    hooks:
      - id: ruff-check
        args: [--fix]
      - id: ruff-format

  - repo: local
    hooks:
      - id: stage-formatted
        name: stage formatted files
        entry: git add -u
        language: system
        pass_filenames: false
        stages: [pre-commit]

  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: pixi run pytest tests --ignore=tests/benchmarks -x -q -n auto
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]